"""
Thermal reservoir experiments using Extropic's `thrml` library.

This package provides a thin wrapper that treats a `thrml` EBM as a
high-dimensional "thermal field" plus simple controller loops that
probe different dynamical regimes (e.g., via temperature schedules).
"""


